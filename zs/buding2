模块“补丁二库”。
函数集“<rust>”、“‘参数0’.so”。

函数“套接文件”以“sock_bind__”、“-Z”（路径、代码（参数是连入串，回应是结果值））。
函数“套接文件连”以“sock_conn__”、“-Z”（路径、串、代码）。

函数  “存在值”以“exist_val__”、“-*:''-*:''-Z”（值【区】、……。返回名，名加引号，多个时顿号分隔）。
函数“首存在值”以“exist_val__”、“-*:''-*:' '-Z”（同上，但返回不加引号）。
函数  “包含值”以“exist_val__”、“-*:' '-*:''-Z”。
函数“首包含值”以“exist_val__”、“-*:' '-*:' '-Z”。

函数“base64化文件”以“base64_file__”、“-Z”（路径）。
函数“遍历目录二”以“dir__”、“-Z”（目录、代码、正则匹配、选项（
	s
	a-d
	a-f
	a-h
	a-l
	ah
	n
	f
	P
	）、目录……）。
